# Introduction
This folder stores the React related scripts.

# To build the script
```shell
# Run the following PowerShell script if running on windows that can compile scripts by Vite
# and copy to src/main/resources/static directory of Springboot
 
./build_react.ps1

```